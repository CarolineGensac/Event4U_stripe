<p style="color: green"><%= notice %></p>


    <div class="banner">
      <%= image_tag("Defqon_nuit.jpg", alt: "Description de l'image") %>
      <h1>Event4U</h1>
    </div>




  <div class="container">
    <h1 class="display-6"> Are you ready to dance ? <%#= current_user.first_name%> </h1>
      <div id="events">
        <% @events.each do |event| %>
          <div class="card" style="width: 18rem;">
            <img src="..." class="card-img-top" alt="...">
              <div class="card-body">
                <h5 class="card-title"><%= event.title %></h5>
                  <p class="card-text"><%= event.description %></p>
                <a href="<%= event_path(event) %>" class="btn">
                  <button type="button" class="btn btn-dark">Voir plus</button>
                </a>
              </div>
          </div>
        <% end %>
      </div>
  </div>

<%= link_to "New event", new_event_path %>
